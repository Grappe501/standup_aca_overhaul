---
import SiteLayout from "../layouts/SiteLayout.astro";
---

<SiteLayout title="For Congress — Stand Up Arkansas">
  <main class="congress">
    <!-- Header -->
    <header class="header" aria-label="Congress staff view">
      <div class="headerInner">
        <div class="headerLeft">
          <p class="eyebrow">For Congress</p>

          <!-- WHY (implicit): purpose + stakes -->
          <h1 class="h1">A staff-ready view of the ACA Overhaul</h1>
          <p class="sub">
            Coverage should be stable, costs should be predictable, and people should be able to understand what they’re buying.
            This entry point is structured for legislative review: clear framing, durable architecture, and section-by-section access.
            (Content will be populated from DOCX next.)
          </p>

          <div class="ctas" aria-label="Primary actions">
            <a class="btn primary" href="/the-problem">Start with The Problem</a>
            <a class="btn" href="/the-backbone">Then The Backbone</a>
            <a class="btn ghost" href="/proposal">Open Proposal</a>
          </div>

          <div class="meta" aria-label="Design signals">
            <div class="meta-pill">
              <span class="dot neutral" aria-hidden="true"></span>
              <span>Plain-language structure</span>
            </div>
            <div class="meta-pill">
              <span class="dot neutral" aria-hidden="true"></span>
              <span>Durability-first framing</span>
            </div>
            <div class="meta-pill">
              <span class="dot draft" aria-hidden="true"></span>
              <span>Working draft</span>
            </div>
          </div>
        </div>

        <!-- HOW (implicit): how to use + mechanism -->
        <aside class="headerRight" aria-label="How to use this view">
          <div class="panel">
            <h2 class="panelTitle">How to review</h2>

            <div class="panelGrid" role="list" aria-label="Core map">
              <a class="mini" href="/the-problem" role="listitem">
                <span class="miniTop">WHY</span>
                <span class="miniMain">The Problem</span>
              </a>
              <a class="mini" href="/the-backbone" role="listitem">
                <span class="miniTop">HOW</span>
                <span class="miniMain">The Backbone</span>
              </a>
              <a class="mini" href="/proposal" role="listitem">
                <span class="miniTop">WHAT</span>
                <span class="miniMain">The Proposal</span>
              </a>
            </div>

            <ol class="steps">
              <li>
                <strong>Start with the framing</strong>
                <span>Goals, constraints, and what “durable” means in practice.</span>
              </li>
              <li>
                <strong>Check the mechanism</strong>
                <span>The Backbone: stability, incentives, and guardrails.</span>
              </li>
              <li>
                <strong>Open the deliverable</strong>
                <span>The Proposal, section-by-section, for citation and reuse.</span>
              </li>
            </ol>

            <div class="panelFoot">
              <span class="pill">
                <span class="dot draft" aria-hidden="true"></span>
                Congress landing shell
              </span>
              <a class="btn" href="/proposal">Sections →</a>
            </div>
          </div>
        </aside>
      </div>

      <div class="headerGlow" aria-hidden="true"></div>
    </header>

    <!-- Routes -->
    <section class="routes" aria-label="Congress navigation">
      <div class="sectionHead">
        <h2 class="sectionTitle">Navigate</h2>
        <p class="sectionKicker">
          Fast entry points for review and sharing. Each page stands alone.
        </p>
      </div>

      <div class="grid">
        <a class="cardlink" href="/start-here">
          <div class="cardTop">
            <p class="cardEyebrow">Orientation</p>
            <h3 class="cardTitle">Start Here</h3>
            <p class="cardDesc">A guided overview of the logic and structure.</p>
          </div>
          <div class="cardFoot"><span class="arrow">Open</span></div>
        </a>

        <a class="cardlink" href="/the-problem">
          <div class="cardTop">
            <p class="cardEyebrow">Context</p>
            <h3 class="cardTitle">The Problem</h3>
            <p class="cardDesc">Constraints, failure modes, and why they persist.</p>
          </div>
          <div class="cardFoot"><span class="arrow">Open</span></div>
        </a>

        <a class="cardlink" href="/the-backbone">
          <div class="cardTop">
            <p class="cardEyebrow">Mechanism</p>
            <h3 class="cardTitle">The Backbone</h3>
            <p class="cardDesc">Durable core architecture: stability, incentives, guardrails.</p>
          </div>
          <div class="cardFoot"><span class="arrow">Open</span></div>
        </a>

        <a class="cardlink" href="/proposal">
          <div class="cardTop">
            <p class="cardEyebrow">Deliverable</p>
            <h3 class="cardTitle">The Proposal</h3>
            <p class="cardDesc">Full plan, section-by-section, built for citation and reuse.</p>
          </div>
          <div class="cardFoot"><span class="arrow">Open</span></div>
        </a>

        <a class="cardlink" href="/library">
          <div class="cardTop">
            <p class="cardEyebrow">Index</p>
            <h3 class="cardTitle">Library</h3>
            <p class="cardDesc">All documents currently available across the site.</p>
          </div>
          <div class="cardFoot"><span class="arrow">Open</span></div>
        </a>

        <div class="cardnote" aria-label="Placeholder note">
          <p class="noteTop">Next</p>
          <p class="noteMain">DOCX ingestion</p>
          <p class="noteSub">
            We’ll populate this page with the Congress-facing framing section, key definitions, and references once the DOCX sources are ingested.
          </p>
          <div class="noteCtas">
            <a class="btn primary" href="/start-here">Overview</a>
            <a class="btn" href="/proposal">Proposal</a>
          </div>
        </div>
      </div>
    </section>

    <!-- Final -->
    <section class="final" aria-label="Final call to action">
      <div class="finalInner">
        <div class="finalCopy">
          <h2 class="finalTitle">Need the fastest orientation?</h2>
          <p class="finalDesc">
            Use the guided overview, then review the Proposal section-by-section with the Backbone as the reference frame.
          </p>
        </div>
        <div class="finalCta">
          <a class="btn primary" href="/start-here">Start Here</a>
          <a class="btn" href="/proposal">Open Proposal</a>
        </div>
      </div>
    </section>
  </main>

  <style>
    .congress{
      display:flex;
      flex-direction:column;
      gap: 18px;
      padding: 6px 0 18px;
    }

    /* Header (token-safe) */
    .header{
      position: relative;
      border-radius: 18px;
      padding: 18px;
      border: 1px solid var(--stroke);
      background:
        radial-gradient(1200px 420px at 20% 0%, rgba(var(--brand), .14), transparent 60%),
        radial-gradient(900px 460px at 90% 20%, rgba(var(--brand2), .10), transparent 55%),
        var(--surface);
      overflow:hidden;
      box-shadow: var(--shadow-sm);
    }

    .headerGlow{
      position:absolute;
      inset:auto -10% -60% -10%;
      height: 260px;
      background:
        radial-gradient(closest-side, rgba(var(--brand), .20), transparent 70%),
        radial-gradient(closest-side, rgba(var(--brand2), .12), transparent 70%);
      filter: blur(12px);
      opacity: .75;
      pointer-events:none;
    }

    .headerInner{
      position: relative;
      display:grid;
      grid-template-columns: 1.2fr 0.9fr;
      gap: 16px;
      align-items:start;
      z-index: 1;
    }

    .eyebrow{
      margin:0 0 8px;
      font-size: 12px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--text-subtle);
    }

    .h1{
      margin:0;
      font-size: clamp(30px, 3.2vw, 44px);
      line-height: 1.07;
      letter-spacing: -0.02em;
      color: var(--text);
    }

    .sub{
      margin: 10px 0 0;
      font-size: 14px;
      line-height: 1.6;
      color: var(--text-muted);
      max-width: 78ch;
    }

    .ctas{
      margin-top: 14px;
      display:flex;
      gap: 10px;
      flex-wrap:wrap;
      align-items:center;
    }

    :global(.btn.ghost){
      background: transparent;
      border: 1px solid var(--stroke-strong);
    }
    :global(.btn.ghost:hover){
      border-color: rgba(var(--ring), .55);
      transform: translateY(-1px);
      background: var(--hover);
    }

    .meta{
      margin-top: 12px;
      display:flex;
      flex-wrap:wrap;
      gap: 8px;
    }

    .meta-pill{
      display:inline-flex;
      align-items:center;
      gap: 8px;
      padding: 8px 10px;
      border-radius: 999px;
      border: 1px solid var(--stroke);
      background: var(--surface2);
      font-size: 13px;
      color: var(--text-muted);
      white-space: nowrap;
    }

    .dot{
      width: 8px;
      height: 8px;
      border-radius: 999px;
    }
    .dot.neutral{
      background: rgba(var(--fg), .65);
      box-shadow: 0 0 0 3px rgba(var(--fg), .10);
    }
    .dot.draft{
      background: rgba(var(--warn), .95);
      box-shadow: 0 0 0 6px rgba(var(--warn), .12);
    }

    /* Panel */
    .panel{
      border-radius: 16px;
      padding: 14px;
      border: 1px solid var(--stroke);
      background: var(--surface2);
      backdrop-filter: blur(10px);
      box-shadow: var(--shadow-sm);
    }

    .panelTitle{
      margin:0;
      font-size: 14px;
      letter-spacing: -0.01em;
      color: var(--text);
    }

    .panelGrid{
      margin: 10px 0 12px;
      display:grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 8px;
    }

    .mini{
      text-decoration:none;
      color: inherit;
      border-radius: 14px;
      padding: 10px;
      border: 1px solid var(--stroke);
      background: var(--surface);
      transition: transform .14s ease, border-color .14s ease, background .14s ease;
      display:grid;
      gap: 4px;
    }

    .mini:hover{
      transform: translateY(-1px);
      border-color: rgba(var(--ring), .55);
      background: var(--hover);
    }

    .miniTop{
      font-size: 11px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--text-subtle);
    }

    .miniMain{
      font-size: 13px;
      font-weight: 800;
      letter-spacing: -0.01em;
      color: var(--text);
    }

    .steps{
      margin: 0;
      padding-left: 18px;
      display:grid;
      gap: 10px;
      color: var(--text-muted);
    }

    .steps li strong{
      display:block;
      font-weight: 800;
      color: var(--text);
    }

    .steps li span{
      display:block;
      color: var(--text-muted);
      font-size: 13px;
      line-height: 1.45;
      margin-top: 2px;
    }

    .panelFoot{
      margin-top: 12px;
      padding-top: 12px;
      border-top: 1px solid var(--stroke);
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 10px;
      flex-wrap:wrap;
    }

    .pill{
      display:inline-flex;
      align-items:center;
      gap: 8px;
      padding: 6px 10px;
      border-radius: 999px;
      border: 1px solid var(--stroke);
      background: var(--surface);
      font-size: 12px;
      color: var(--text-muted);
      white-space: nowrap;
    }

    /* Routes */
    .sectionHead{
      display:grid;
      gap: 6px;
      padding: 0 2px;
    }

    .sectionTitle{
      margin: 0;
      font-size: 18px;
      letter-spacing: -0.01em;
      color: var(--text);
    }

    .sectionKicker{
      margin: 0;
      color: var(--text-muted);
      font-size: 14px;
      line-height: 1.5;
      max-width: 84ch;
    }

    .grid{
      margin-top: 14px;
      display:grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 12px;
    }

    .cardlink{
      display:grid;
      gap: 12px;
      padding: 16px;
      border-radius: 16px;
      border: 1px solid var(--stroke);
      background: var(--surface);
      text-decoration:none;
      color: inherit;
      box-shadow: var(--shadow-sm);
      transition: transform 140ms ease, border-color 140ms ease, background 140ms ease;
    }

    .cardlink:hover{
      transform: translateY(-2px);
      border-color: rgba(var(--ring), .55);
      background: var(--hover);
    }

    .cardEyebrow{
      margin: 0 0 6px;
      font-size: 12px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--text-subtle);
    }

    .cardTitle{
      margin: 0;
      font-size: 16px;
      letter-spacing: -0.01em;
      color: var(--text);
    }

    .cardDesc{
      margin: 6px 0 0;
      font-size: 14px;
      line-height: 1.55;
      color: var(--text-muted);
    }

    .cardFoot{
      display:flex;
      justify-content:flex-end;
      align-items:center;
      padding-top: 2px;
      color: var(--text);
      font-weight: 800;
    }

    .arrow::after{
      content: " →";
      opacity: 0.9;
    }

    .cardnote{
      border-radius: 16px;
      padding: 16px;
      border: 1px solid var(--stroke);
      background: var(--surface2);
      display:grid;
      gap: 8px;
      box-shadow: var(--shadow-sm);
    }

    .noteTop{
      margin:0;
      font-size: 12px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--text-subtle);
    }

    .noteMain{
      margin:0;
      font-size: 16px;
      font-weight: 850;
      letter-spacing: -0.01em;
      color: var(--text);
    }

    .noteSub{
      margin: 0;
      font-size: 13px;
      line-height: 1.5;
      color: var(--text-muted);
    }

    .noteCtas{
      margin-top: 6px;
      display:flex;
      gap: 10px;
      flex-wrap:wrap;
    }

    /* Final */
    .final{
      border-radius: 18px;
      padding: 16px;
      border: 1px solid var(--stroke);
      background: var(--surface);
      box-shadow: var(--shadow-sm);
    }

    .finalInner{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 14px;
      flex-wrap:wrap;
    }

    .finalTitle{
      margin: 0;
      font-size: 18px;
      letter-spacing: -0.01em;
      color: var(--text);
    }

    .finalDesc{
      margin: 6px 0 0;
      font-size: 14px;
      line-height: 1.55;
      color: var(--text-muted);
      max-width: 72ch;
    }

    .finalCta{
      display:flex;
      gap: 10px;
      flex-wrap:wrap;
      align-items:center;
    }

    @media (max-width: 980px){
      .headerInner{ grid-template-columns: 1fr; }
      .grid{ grid-template-columns: 1fr; }
      .panelGrid{ grid-template-columns: 1fr; }
    }
  </style>
</SiteLayout>
